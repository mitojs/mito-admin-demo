import { Button } from 'antd'
import React from 'react'
import styled from 'styled-components'
const PlainLinkButton = styled(Button)`
  border: none;
  box-shadow: none;
`
export default PlainLinkButton
